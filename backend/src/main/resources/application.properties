server.port=8080

# Database
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:kanban_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Dinesh}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Email SMTP Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:dineshpradhan0305@gmail.com}
spring.mail.password=${MAIL_PASSWORD:uctuopdmqitxozcm}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.test-connection=false

# JWT Configuration
app.jwt.secret=${JWT_SECRET:myVeryLongSecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLong2024}
app.jwt.access-token-expiration=900000
app.jwt.refresh-token-expiration=604800000

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:5175}

# Email App Settings
app.mail.enabled=${MAIL_ENABLED:true}
app.mail.from=${MAIL_FROM:dineshpradhan0305@gmail.com}
app.mail.frontend-url=${FRONTEND_URL:http://localhost:5173}

# Invitations
app.invitation.expires-days=${INVITATION_EXPIRES_DAYS:14}
